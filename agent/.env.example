# Copy to .env (never commit secrets)
# Free offline demo (no Anthropic credits):
AGENT_DEMO_MODE=1
# Optional — only needed when AGENT_DEMO_MODE is off:
# AGENT_ANTHROPIC_API_KEY=sk-ant-your-key-here
AGENT_PORT=8787
